# Sessão 2026-05-14

## Knowledge Base + Prompt Management
- **Bug fix crítico**: `routes/seats.js` tinha `(l: any)` (TS annotation em arquivo .js) — quebrava todos os endpoints Seats.aero
- **Novo**: link Google Flights em `StrategyContent.tsx` quando `vale_a_pena === false`
- **Novo**: prompts extraídos de `strategy/index.ts` e `chat-busca/index.ts` para `prompts/*.ts` — editáveis sem tocar lógica de negócio
- **Novo**: migration `036_knowledge_base.sql` — tabelas `knowledge_base` e `knowledge_inbox` (aplicada no Supabase)
- **Novo**: `scripts/sync-knowledge.js` — sincroniza vault Obsidian → Supabase
- **Novo**: `fetchKnowledge()` em ambas as Edge Functions — RAG por tags injetado no prompt
- **Novo**: `knowledge/` — vault Obsidian com 3 notas seed (Smiles, LATAM Pass, conceito CPM)
- **Novo**: git hook `pre-push` — sincroniza `knowledge/` automaticamente no push

## Migração OpenAI → Claude
- **Migrado**: `strategy/index.ts` — GPT-4o-mini → Claude Haiku 4.5
- **Migrado**: `refresh-extras/index.ts` — GPT-4o-mini → Claude Haiku 4.5
- **Projeto 100% Claude** — zero dependência de OpenAI; `OPENAI_API_KEY` não é mais necessária

## Limpeza e Organização
- Removidos `FLYWISE_NOTAS.md`, `FLYWISE_CONTEXT.md`, `FLYWISE_DESIGN_SYSTEM.md`, `CLAUDE_PROJECT_INSTRUCTIONS.md` da raiz — conteúdo mesclado em `flywise-project-context.md`
- Removidos plano e spec do Remotion (`docs/superpowers/plans/` e `specs/`)
- Removida skill `remotion-best-practices` de `.agents/` e `.claude/`
- Pasta lixo `Fly Wise/` criada pelo Obsidian removida

## Auditoria e Correções do Contexto
- Varredura completa do codebase revelou dados incorretos no contexto:
  - `checkout.js` corrigido para `payments.js`
  - Tiers de plano corrigidos (pro=5/mês, elite=10/mês — estavam como "ilimitado")
  - Edge Function `itinerary/` (Claude Sonnet 4.6) estava faltando
  - 3 GitHub Actions faltando: `sync-transfer-data.yml`, `watchlist-check.yml`, `refetch.yml`
  - `scrape_melhores_destinos.py` adicionado ao mapa de arquivos

## Reorganização do Contexto
- Histórico de mudanças movido para `docs/context/sessions/YYYY-MM-DD.md`
- Arquivo principal (`flywise-project-context.md`) agora reflete apenas estado atual — sem acúmulo de histórico
